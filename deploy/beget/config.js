/**
 * Адрес бэкенда (FastAPI). Без слэша в конце.
 * Если фронт и бэкенд на одном домене — оставьте пустую строку.
 * Если бэкенд на другом хостинге (Railway, Render и т.п.) — укажите полный URL.
 */
window.MATRIX_API_BASE = '';
